/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  siteUrl: 'https://periciadigital.tec.br/', // Substitua pela URL do seu site
  generateRobotsTxt: true, // Gera um robots.txt automaticamente
  // outras configurações
};

export default nextConfig;
